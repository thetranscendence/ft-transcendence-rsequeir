# ==============================================================================
# TEMPLATE DE CONFIGURATION (.env.example)
# ==============================================================================
# CE FICHIER EST PUBLIC ET DOIT ÊTRE COMMITTÉ SUR GIT.
# Il sert de modèle pour créer votre fichier de configuration locale (.env).
#
# INSTRUCTIONS :
# 1. Copiez ce fichier à la racine du projet :
#    $ cp .env.example .env
# 2. Modifiez les valeurs dans le nouveau fichier .env (surtout VAULT_ROOT_TOKEN).
#
# ℹ️ NOTE SUR L'ARCHITECTURE DE SÉCURITÉ (ZERO-TRUST) :
# Conformément aux exigences de sécurité (IV.4), aucun mot de passe de service
# (DB, Broker) n'est défini dans ce fichier. Ils seront générés cryptographiquement
# lors du premier lancement par le conteneur 'vault-init' et injectés via des
# volumes en mémoire vive (tmpfs).
# ==============================================================================

# --- HASHICORP VAULT (INFRASTRUCTURE) ---
# Token d'administration racine pour Vault en mode développement.
# Dans votre .env local, remplacez cette valeur par une chaîne aléatoire unique.
VAULT_ROOT_TOKEN=change_me_to_a_secure_token_in_local_env

# --- RABBITMQ (MESSAGERIE ASYNCHRONE) ---
# Nom d'utilisateur par défaut pour l'administration du broker.
# Le mot de passe associé sera généré automatiquement.
RABBITMQ_USER=guest

# --- POSTGRESQL (BASE DE DONNÉES) ---
# Nom du super-utilisateur pour la base de données principale.
# Le mot de passe associé sera généré automatiquement.
POSTGRES_USER=postgres

# --- NODEJS & APPLICATION (GLOBAL) ---
# Contexte d'exécution de l'application.
# Valeurs possibles : 'development' (par défaut), 'test', 'production'.
NODE_ENV=development